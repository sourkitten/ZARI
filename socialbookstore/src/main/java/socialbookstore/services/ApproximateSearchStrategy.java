package socialbookstore.services;

import java.util.List;

import socialbookstore.domainmodel.Book;

public class ApproximateSearchStrategy extends TemplateSearchStrategy {
    /*@Override
    protected List<BookFormData> initializeListOfBooks(SearchFormData searchFormData) {
        // Logic to initialize book list approximately
        //return bookMapper.findBooksApproximately(searchFormData);
    }

    @Override
    protected boolean checkIfAuthorMatches(SearchFormData searchFormData, Book book) {
        // Logic for approximate matching of authors
        //return book.getBookAuthors().contains(searchFormData.getAuthorKeyword());
    }*/
}

