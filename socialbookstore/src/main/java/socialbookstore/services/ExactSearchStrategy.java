package socialbookstore.services;

import java.util.List;

import socialbookstore.domainmodel.Book;

public class ExactSearchStrategy extends TemplateSearchStrategy {
	/*@Override
    protected List<BookFormData> initializeListOfBooks(SearchFormData searchFormData) {
        // Logic to initialize book list exactly
        //return bookMapper.findBooksExactly(searchFormData);
    }

    @Override
    protected boolean checkIfAuthorMatches(SearchFormData searchFormData, Book book) {
        // Logic for exact matching of authors
        //return book.getBookAuthors().equals(searchFormData.getAuthorKeyword());
    }*/
}

